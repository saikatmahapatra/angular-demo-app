import { CheckCapsLockDirective } from './check-caps-lock.directive';

describe('CheckCapsLockDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckCapsLockDirective();
    expect(directive).toBeTruthy();
  });
});
