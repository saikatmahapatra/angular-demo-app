export enum EnumStatus {
    Y = 'Active',
    N = 'Inactive',
    A = 'Archived',
}

export enum EnumLeveType {
    PL = 'PL-Privileged Leave',
    CL = 'CL-Casual Leave',
    SL = 'SL-Sick Leave',
}