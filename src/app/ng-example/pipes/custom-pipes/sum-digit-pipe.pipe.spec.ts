import { SumDigitPipePipe } from './sum-digit-pipe.pipe';

describe('SumDigitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SumDigitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
