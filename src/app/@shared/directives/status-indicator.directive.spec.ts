import { StatusIndicatorDirective } from './status-indicator.directive';

describe('StatusIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
